<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="styles.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="app.js"></script>
  </head>
  <body>
    <div id="header">
        <h1>Hazmat Newspaper</h1>
        <p>Here are the posts from the Radiation blog that you currently follow.</p>
        <p>Use keys j and k and navigate between posts. Then, to scroll within the selected post, use the arrow keys. To visit the selected post, hit enter.</p>
    </div>
    

    <!-- Here is where your blog posts get generated. -->
    <div id="blog">
        <% @posts_array.each do |post| %>
            <div class="blog_post" id="<%= post.file_name %>" tabindex="-1">              
              Post by <a href="<%= post.base_url %>">~<%= post.user_name %></a>
                <br>
                <a class="permalink" target="_new" href="<%= post.perma_url %>">Permalink</a>
                <div>
           
                <%= post.content %>
                </div>
               <!--  <div class="date">
                  <a href="<%#= post.perma_url %>">Read More</a>
                </div> -->
            </div>
        <% end %>
    </div>

    <!-- You could put a footer down here, after all of you blog posts -->
  </body>
</html>
